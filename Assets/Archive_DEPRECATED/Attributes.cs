using UnityEngine;

public class Attributes : MonoBehaviour
{
    /*
    [Header("Player Settings")]
    [SerializeField]
    private float speed = 5;

    [Tooltip("Health value from range 0 to 100")]
    [Range(0, 100)]
    public int health;

    [Header("World Settings")]
    [Tooltip("Gravity value to be applied to player settings")]
    [Range(0.0f, 9.8f)]
    public float gravity;
    */

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
